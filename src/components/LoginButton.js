import React from 'react'
import { Link, Button } from 'react-materialize'

const LoginButton = () => { return <Link to="/login">
                                <Button> log in </Button>
                            </Link>}

